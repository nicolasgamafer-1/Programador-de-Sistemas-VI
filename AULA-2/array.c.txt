#include <stdio.h>
 
int main() {

char nome [25];
double salario=0.0, vendas=0.0, total=0.0;

scanf("%s", &nome);
scanf("%lf", &salario);
scanf("%lf", &vendas);

total = salario+(vendas*0.15);

printf("TOTAL = R$ %.2lf\n", total);
 
    return 0;
}